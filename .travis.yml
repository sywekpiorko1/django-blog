language: python 
matrix:
    include:
        - python: 3.4
          dist: trusty
          sudo: false
        - python: 3.5
          dist: trusty
          sudo: false
        - python: 3.6
          dist: trusty
          sudo: false
        - python: 3.7
          dist: xenial
sudo: true
install: "pip install -t requirements.txt"
script:
- SECRET_KEY="whatever" ./manage.py test